function Title() {
  return (
    <h1
      className="m-0"
      style={{
        backgroundColor: "red",
        textAlign: "center",
        padding: "1rem",
        color: "#fff",
      }}
    >
      Formulario
    </h1>
  );
}

export default Title;
